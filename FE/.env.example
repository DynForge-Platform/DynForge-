# URL của backend API (không có dấu / ở cuối)
# Dev: để trống sẽ dùng http://localhost:8080
# Production (Vercel): set trong Project Settings > Environment Variables
VITE_API_URL=http://localhost:8080
